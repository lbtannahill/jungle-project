<h1>Admin : Dashboard</h1>

<div class="panel panel-default items" style="text-align: center;">
<h3>Current Inventory</h3>
<h4>Products in Database: <%= @products.count %></h4>
<button type="button" class="btn btn-primary"><%= link_to "Products" , [:admin, :products], {:style=>'color:#FFFFFF;'}%></button>
<h4>Categories in Database: <%= @categories.count %></h4>
 <% Category.all.each do |category| %>
  <button type="button" class="btn btn-info"><%= link_to category.name, category, {:style=>'color:#FFFFFF;'} %></button>
   <% end %>

  </div>